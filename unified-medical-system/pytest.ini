[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v -s --strict-markers
markers =
    ui: marks tests as UI tests (using Selenium)
    unit: marks tests as unit tests
    integration: marks tests as integration tests 